# Prometheus Configuration
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'infrastructure-monitor'

# Alerting
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

# Scrape configurations
scrape_configs:
  # FastAPI Backend
  - job_name: 'fastapi'
    static_configs:
      - targets: ['backend:8000']
        labels:
          service: 'fastapi-backend'
    metrics_path: '/metrics'
    scrape_interval: 10s
  
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  # PostgreSQL Exporter (opcional)
  # - job_name: 'postgresql'
  #   static_configs:
  #     - targets: ['postgres-exporter:9187']
  
  # Redis Exporter (opcional)
  # - job_name: 'redis'
  #   static_configs:
  #     - targets: ['redis-exporter:9121']
  
  # Node Exporter (opcional - para métricas del host)
  # - job_name: 'node'
  #   static_configs:
  #     - targets: ['node-exporter:9100']
